# 2026-03-18 Codex 作業記録

## 目的

GAS で実装されている会計システムについて、以下を同時に満たす改修を行った。

1. 支出申請画面と精算画面の文言・入力制御改善
2. 事前請求 / 事後請求の重複申請防止
3. 管理者画面の検索 / 並び替え機能追加
4. スパゲッティコード回避のための責務分割
5. 自動テスト追加

今回の変更は、ユーザー指示に従い基本的に `src` 配下を実装対象とし、テストは `test` ディレクトリに追加した。  
この作業記録ファイル `2026-03-18-codex.md` は引き継ぎ用としてルートに作成・更新している。

---

## 作業前に読んだファイル

最初に以下をすべて読み、既存の責務とデータフローを把握した。

- `src/admin.html`
- `src/code.ts`
- `src/css.html`
- `src/index.html`
- `src/javascript.html`
- `src/select_role.html`
- `build.js`
- `package.json`
- `tsconfig.json`
- `eslint.config.mjs`

確認時点の所見:

- GAS のエントリは `src/code.ts`
- HTML は `include()` で `css.html`, `javascript.html` を差し込む構成
- 管理者画面は `admin.html` にロジックが直書き
- ユーザー画面は `index.html` + `javascript.html` で動作
- 業務ルール、シート I/O、画面用データ整形が `code.ts` に混在
- テスト実行基盤は未整備で、`package.json` の `test` スクリプトは未設定

---

## 実施した要件対応

### 1. 支出申請ページの申請タイプ選択から「事前申請の精算」を削除

対応内容:

- `src/index.html` の申請フォームから `精算` 用 option を削除
- `src/javascript.html` 側で、支出申請モードと精算モードを別状態として管理
- 精算は「精算する」ボタンからのみ開始する方式に変更

結果:

- 通常の支出申請画面では `事後` と `事前` のみ選択可能
- `精算` は UI 上で申請タイプとして選べない

### 2. 「証憑(レシート)」を「領収書(レシート)」に変更

対応内容:

- ユーザー画面の文言を `領収書(レシート)` に統一
- 管理者画面の一覧見出しも `領収書` に変更
- 共通文言は `src/accounting-domain.ts` の `RECEIPT_LABEL` に集約

### 3. 添付ファイルを PDF のみに制限

対応内容:

- クライアント側:
  - `src/index.html` の `input[type=file]` の `accept` を `application/pdf` に変更
  - `src/javascript.html` で `.pdf` または `application/pdf` 以外を弾く
- サーバー側:
  - `src/expenditure-policy.ts` の `validateAttachment()` で最終検証
  - `事後` / `精算` は PDF 必須
  - `事前` は添付不要

結果:

- フロントでもバックでも PDF 以外は拒否
- 将来フロントを迂回してもサーバー側で防げる

### 4. 精算手続きページの申請タイプ欄を削除

対応内容:

- `src/index.html` に申請タイプ欄コンテナ `typeSelectGroup` を残しつつ
- `src/javascript.html` の精算モードで `display:none` に制御

結果:

- 精算開始時は申請タイプ欄が見えない
- 精算は `requestType = '精算'` を内部的に固定して送信

### 5. 精算手続きページの文言変更

対応内容:

- 精算時の添付欄ラベルも `領収書(レシート)` に統一

### 6. 事前請求がある状態で追加の事前請求を禁止

対応内容:

- `src/expenditure-policy.ts` に業務ルールを移設
- 同一団体の `事前` レコードについて、以下のいずれかのステータスが存在する場合に新規 `事前` を禁止:
  - `申請中`
  - `承認済`
  - `未精算`

制御箇所:

- サーバー: `ExpenditureRequestPolicy.assertCanCreateRequest()`
- 画面: `formBootstrap.requestAvailability` を使って選択肢を disabled 化

### 7. 事後請求がある状態で追加の事後請求を禁止

対応内容:

- 同一団体の `事後` レコードに `申請中` がある場合のみ新規 `事後` を禁止
- `承認済`, `却下`, `精算完了` などはブロック条件に含めない

制御箇所:

- サーバー: `ExpenditureRequestPolicy.assertCanCreateRequest()`
- 画面: `requestAvailability` をもとに `事後` option を disabled 化

### 8. 管理者ページに検索機能を追加

追加した条件:

- ID 検索
- 日付検索
  - 開始日
  - 終了日
  - `input type="date"` を使用
- 団体名検索
  - 複数選択可
- タイプ検索
  - 複数選択可
- 金額検索
  - 指定なし
  - 一致
  - 以上
  - 以下
  - 範囲
- 内容検索
  - キーワード部分一致

並び替え:

- 日付: 昇順 / 降順
- ID: 昇順 / 降順

実装方針:

- 画面側: `src/admin.html` と `src/admin_script.html`
- 検索ロジック: `src/admin-dashboard-service.ts`
- サーバー API: `src/code.ts` の `getAdminDashboardData(criteriaInput)`

### 9. 機能改善後のテスト追加

追加テスト:

- `test/expenditure-policy.test.ts`
- `test/admin-dashboard-service.test.ts`
- `test/user-form-view-model.test.ts`
- `test/helpers.ts`

---

## リファクタリング内容

今回の主目的は機能追加だけでなく、保守しやすい形に整理することだったため、以下の責務分離を行った。

### `src/accounting-domain.ts`

用途:

- ドメイン定数
- 型定義
- 純粋ユーティリティ

代表要素:

- `APPLICATION_TYPE_OPTIONS`
- `RECEIPT_LABEL`
- `PDF_MIME_TYPE`
- `ExpenditureRecord`
- `RequestAvailabilityMap`
- `toDateKey()`
- `stripDetailSuffix()`

### `src/expenditure-policy.ts`

用途:

- 支出 / 精算まわりの業務ルール

主な責務:

- 申請可否判定
- 添付必須判定
- PDF 添付検証
- 未精算の事前請求取得
- 履歴 / 残高 / 申請可否サマリ生成

重要メソッド:

- `ExpenditureRequestPolicy.getRequestAvailability()`
- `ExpenditureRequestPolicy.assertCanCreateRequest()`
- `ExpenditureRequestPolicy.assertCanStartSettlement()`
- `ExpenditureRequestPolicy.validateAttachment()`
- `ExpenditureRequestPolicy.summarizeGroupRecords()`

### `src/admin-dashboard-service.ts`

用途:

- 管理者検索条件の正規化
- 検索
- 並び替え
- フィルタ選択候補生成

重要メソッド:

- `normalizeCriteria()`
- `filterAndSort()`
- `getFilterOptions()`

### `src/user-form-view-model.ts`

用途:

- ユーザー画面のフォーム状態を組み立てる純粋ロジック

主な責務:

- 支出申請モード / 精算モードの差分整理
- 表示する申請タイプ一覧生成
- 添付欄の表示要否判定

重要メソッド:

- `UserFormViewModelFactory.buildBootstrap()`
- `UserFormViewModelFactory.buildState()`
- `UserFormViewModelFactory.hasAvailableApplicationType()`

### `src/code.ts`

役割を薄くした内容:

- GAS エントリポイント
- シート入出力
- メール送信
- HTML レンダリング
- 上記サービス / ポリシー呼び出し

以前より改善した点:

- 画面用ルール判定を別ファイルへ分離
- 管理者検索を `code.ts` に直書きしない構成へ変更
- ユーザー画面初期表示用データを `formBootstrap` として明示的に返却

### `src/javascript.html`

変更方針:

- 画面制御をクラス化
- テーブル制御と画面制御を分離

クラス:

- `ItemsTableController`
  - 明細行追加 / 削除
  - 小計 / 合計再計算
  - 事前申請時のプルダウン制御
  - 重複品目防止
- `UserDashboardApp`
  - ダッシュボード / フォーム / 問い合わせの画面遷移
  - 支出申請モード / 精算モード切替
  - 申請可能タイプの反映
  - PDF 添付チェック
  - submit 処理

### `src/admin_script.html`

新規追加理由:

- `admin.html` に管理ロジックを埋め込むと見通しが悪くなるため、検索 UI とロジックを分離

クラス:

- `AdminDashboardApp`
  - 検索条件の保持
  - 検索条件 UI の収集 / 反映
  - 結果レンダリング
  - ステータス更新後の再読込

---

## ファイル別の変更点

### `src/index.html`

主な変更:

- 申請タイプ `select` を空の状態で置き、JS 側で動的構築
- 精算時に申請タイプ欄を隠せる構造へ変更
- 添付欄ラベルを `領収書(レシート)` に変更
- `accept="application/pdf"` を設定
- `USER_FORM_BOOTSTRAP` を埋め込むように変更
- 申請制限メッセージ表示ボックスを追加

### `src/javascript.html`

主な変更:

- 旧関数群をクラス化
- `精算` を直接選ばせる UI を廃止
- 申請可能タイプが 0 件なら支出申請開始を禁止
- 精算対象がない場合は精算開始を禁止
- 添付ファイル PDF チェック追加
- フォーム状態に応じて:
  - 申請タイプ欄表示 / 非表示
  - 添付欄表示 / 非表示
  - 添付必須 / 任意
  - ボタン文言
  - お釣り / 不足表示
  を切り替えるように変更

### `src/admin.html`

主な変更:

- 管理者検索 UI 追加
- 団体名 / タイプの複数選択欄追加
- 金額検索モード UI 追加
- ソート UI 追加
- `admin_script` include に差し替え

### `src/admin_script.html`

主な変更:

- 管理者画面用スクリプトを分離
- 条件収集と再描画を一元管理

### `src/code.ts`

主な変更:

- `getAdminDashboardData(criteriaInput)` を検索対応版に変更
- `getUserStatus()` で:
  - 履歴
  - 残高
  - 未精算情報
  - 申請可否
  - フォーム初期表示情報
  をまとめて返すように変更
- `processForm()` で:
  - 申請タイプの妥当性チェック
  - 重複申請禁止
  - 精算開始条件チェック
  - 添付 PDF バリデーション
  を追加

### `src/css.html`

主な変更:

- 警告ボックス用スタイル追加
- disabled ボタンスタイル追加
- 精算情報表示用スタイル追加
- 補助テキスト用スタイル追加

---

## データ仕様と業務ルールの整理

### T_Expenditure の appendRow 仕様

`processForm()` で追加する列順は以下の通り。

1. 申請 ID
2. 登録日時
3. 団体 ID
4. タイプ
5. ステータス
6. 合計金額
7. 内容文字列
8. ファイル URL
9. 固定値 `未`

今回の初期ステータス:

- `事前` の新規申請: `未精算`
- `事後` の新規申請: `申請中`
- `精算` の新規申請: `申請中`

注意:

- 現行仕様上、精算も `T_Expenditure` に別レコード追加する運用のまま維持している
- 既存列 9 の意味は今回深掘りせず、既存仕様に合わせて `未` を維持

### 申請制限ロジック

#### 事前請求

同一 `groupId` 内に以下のいずれかがある場合、新規 `事前` を禁止:

- `type === '事前' && status === '申請中'`
- `type === '事前' && status === '承認済'`
- `type === '事前' && status === '未精算'`

#### 事後請求

同一 `groupId` 内に以下がある場合、新規 `事後` を禁止:

- `type === '事後' && status === '申請中'`

#### 精算開始条件

同一 `groupId` 内に以下がある場合のみ精算可能:

- `type === '事前' && status === '未精算'`

### 残高計算

残高計算で利用済として加算するステータス:

- `承認済`
- `精算完了`

### 添付ルール

- `事前`: 添付不要
- `事後`: PDF 必須
- `精算`: PDF 必須

---

## 管理者検索仕様

### 検索条件オブジェクト

`getAdminDashboardData(criteriaInput)` へ渡している条件は概ね以下。

```ts
{
  idQuery: string,
  dateFrom: string | null,
  dateTo: string | null,
  groups: string[],
  types: string[],
  amountMode: 'none' | 'exact' | 'min' | 'max' | 'range',
  amountValue: number | null,
  amountMin: number | null,
  amountMax: number | null,
  contentQuery: string,
  sortBy: 'date' | 'id',
  sortOrder: 'asc' | 'desc'
}
```

### 金額検索の挙動

- `none`: 金額条件なし
- `exact`: 完全一致
- `min`: 指定額以上
- `max`: 指定額以下
- `range`: `amountMin` 以上かつ `amountMax` 以下

### 文字列検索の挙動

- ID: 部分一致
- 内容: 部分一致
- 正規化は `trim().toLowerCase()` ベース

### 並び替え

- `sortBy = date`
  - 主キー: 日付
  - 副キー: ID
- `sortBy = id`
  - 主キー: ID
  - 副キー: 日付

---

## テスト内容

### `test/expenditure-policy.test.ts`

カバーしている内容:

- 残高 / 履歴 / 未精算抽出
- 事前重複申請禁止
- 事後重複申請禁止
- PDF 添付検証
- 精算開始条件

### `test/admin-dashboard-service.test.ts`

カバーしている内容:

- ID 検索
- 日付範囲検索
- 複数団体検索
- 複数タイプ検索
- 金額:
  - 一致
  - 以上
  - 以下
  - 範囲
- 内容キーワード検索
- 日付ソート
- ID ソート
- フィルタ候補生成

### `test/user-form-view-model.test.ts`

カバーしている内容:

- 支出申請画面の基本状態
- 精算画面の基本状態
- 事後請求不可時のフォールバック
- 両タイプ不可時の申請不可判定
- PDF 受け入れ設定

---

## 実行した確認コマンド

### テスト

```sh
node --experimental-strip-types --test test/*.test.ts
```

結果:

- 18 テスト成功

### ビルド

```sh
npm run build
```

結果:

- 成功
- `build.js` により `src/code.ts` を bundle
- その後 `src/*.html` を `dist/` にコピー

### HTML 内スクリプト構文確認

```sh
node -e "const fs=require('fs'); for (const file of ['src/javascript.html','src/admin_script.html']) { const raw=fs.readFileSync(file,'utf8'); const script=raw.replace(/^<script>\s*/,'').replace(/\s*<\/script>\s*$/,''); new Function(script); } console.log('html script syntax ok');"
```

結果:

- `html script syntax ok`

### 補足

Node テスト時に以下の警告は出る:

- `ExperimentalWarning: Type Stripping is an experimental feature`
- `MODULE_TYPELESS_PACKAGE_JSON`

理由:

- `package.json` に `"type": "module"` を追加していないため
- 今回は `src` / `test` 中心で対応する条件に合わせ、`package.json` は変更していない

---

## 次の作業者向けの注意点

### 1. `src/test.js` は既に削除状態だった

`git status` 確認時点で `src/test.js` は削除済みだった。  
今回こちらでは復元していない。  
この削除がユーザー作業か過去作業かは未確認なので、必要なら履歴確認を行うこと。

### 2. `精算` の扱いは既存仕様を踏襲している

今回の修正では UI 上は精算手続き専用画面に寄せたが、保存先は従来通り `T_Expenditure` のまま。  
事前請求レコード自体を更新して精算完了にする設計には変えていない。  
もし将来「精算時は元の事前申請レコードのステータスも自動変更する」などにしたい場合は、今回の対象外。

### 3. 管理者画面の検索候補は現存データから動的生成

- 団体名候補: `T_Expenditure` の `groupName`
- タイプ候補: `T_Expenditure` の `type`

そのため、候補はデータに存在する値のみ表示される。

### 4. import に `.ts` 拡張子を付与している

理由:

- Node の `--experimental-strip-types` でテスト実行するため

この形でも今回の `npm run build` は成功している。  
将来 build 周りを変更する場合は、拡張子解決ルールに注意すること。

### 5. フロントだけでなくサーバーでも制約をかけている

今回の制約は以下の 2 段構え。

- UI 側で選べない / 送れない
- `processForm()` 側でも再検証

今後 UI を改修する場合でも、サーバー側の業務ルールを外さないこと。

---

## 再現手順

次の担当者が同じ状態を再現したい場合の手順。

1. ブランチ / 作業ツリーを最新状態にする
2. 以下のファイルが存在することを確認
   - `src/accounting-domain.ts`
   - `src/expenditure-policy.ts`
   - `src/admin-dashboard-service.ts`
   - `src/user-form-view-model.ts`
   - `src/admin_script.html`
   - `test/expenditure-policy.test.ts`
   - `test/admin-dashboard-service.test.ts`
   - `test/user-form-view-model.test.ts`
3. テスト実行

```sh
node --experimental-strip-types --test test/*.test.ts
```

4. ビルド実行

```sh
npm run build
```

5. GAS に反映する場合

```sh
npm run push
```

6. 実機確認項目

- 支出申請画面に `精算` の選択肢がない
- ラベルが `領収書(レシート)` になっている
- PDF 以外を添付するとエラーになる
- 未完了の `事前` があると新しい `事前` ができない
- `申請中` の `事後` があると新しい `事後` ができない
- 精算画面では申請タイプ欄が表示されない
- 管理者画面で各検索条件が動作する
- 日付 / ID で昇順降順ソートできる

---

## 今回編集したファイル

既存更新:

- `src/admin.html`
- `src/code.ts`
- `src/css.html`
- `src/index.html`
- `src/javascript.html`

新規追加:

- `src/accounting-domain.ts`
- `src/expenditure-policy.ts`
- `src/admin-dashboard-service.ts`
- `src/user-form-view-model.ts`
- `src/admin_script.html`
- `test/helpers.ts`
- `test/expenditure-policy.test.ts`
- `test/admin-dashboard-service.test.ts`
- `test/user-form-view-model.test.ts`

---

## まとめ

今回の改修では、単純な文言変更だけでなく、

- 業務ルールのサーバー側固定化
- 管理者検索機能の追加
- フロントの状態管理整理
- テスト可能な純粋ロジックへの分離

まで実施した。

次の担当者は、まず `src/expenditure-policy.ts` と `src/admin-dashboard-service.ts` を読むと業務ルールの全体像を早く把握できる。  
画面の挙動を追いたい場合は `src/index.html` → `src/javascript.html`、管理者画面は `src/admin.html` → `src/admin_script.html` の順で追うと理解しやすい。
